# 前端录音上传交互

本 PR 新增浏览器录音和上传交互，前端会把录制到的音频通过 `/api/audio/upload` 上传到后端。

## 使用流程

1. 启动后端。
2. 启动前端。
3. 打开 `http://localhost:5173`。
4. 点击录音按钮，允许浏览器使用麦克风。
5. 再次点击停止录音，前端自动上传音频。
6. 页面展示最近一次上传记录。

## 注意

当前 PR 只验证“录音 -> 上传 -> 后端保存”链路，不包含语音识别。
